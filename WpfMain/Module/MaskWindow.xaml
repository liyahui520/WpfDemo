<hc:Window
    x:Class="WpfMain.Module.MaskWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controlls="clr-namespace:WpfMain.Controlls"
    Title="MainWindow"
    MinWidth="1200"
    MinHeight="750"
    ShowNonClientArea="False"
    ShowTitle="True" 
    WindowStartupLocation="CenterScreen"  
    mc:Ignorable="d"  WindowStyle="None"
    UseLayoutRounding="True"  WindowState="Maximized"  Topmost="True" ResizeMode="NoResize"
    hc:WindowAttach.IgnoreAltF4="True" hc:WindowAttach.ShowInTaskManager="False"
    >

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1" />
    </WindowChrome.WindowChrome>
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Border  Background="#01AAAAAA">
                <ContentPresenter ClipToBounds="True" />
            </Border>
        </ControlTemplate>
    </Window.Template>
    <hc:SimplePanel>

        <InkCanvas EditingMode="None" Name="PART_Canvas" Background="Transparent"/>
        <Rectangle Name="PART_MaskAreaLeft" IsHitTestVisible="False"  Fill="#AA000000" HorizontalAlignment="Left" VerticalAlignment="Top" />
        <Rectangle Name="PART_MaskAreaTop" IsHitTestVisible="False" Fill="#AA000000" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Rectangle Name="PART_MaskAreaRight" IsHitTestVisible="False" Fill="#AA000000" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Rectangle Name="PART_MaskAreaBottom" IsHitTestVisible="False" Fill="#AA000000" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Border Name="PART_TargetArea">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="IsHitTestVisible" Value="False"/>
                    <Setter Property="BorderThickness" Value="5"/>
                    <Setter Property="BorderBrush" Value="DodgerBlue"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalAlignment" Value="Left"/>
                    <Setter Property="VerticalAlignment" Value="Top"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSelecting}" Value="True">
                            <Setter Property="BorderThickness" Value="1"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsDrawing}" Value="True">
                            <Setter Property="IsHitTestVisible" Value="True"/>
                            <Setter Property="BorderThickness" Value="1"/>
                        </DataTrigger>
                    </Style.Triggers>

                </Style>
            </Border.Style>
            <hc:SimplePanel Background="Transparent">
                <hc:SimplePanel.Resources>
                    <Style  TargetType="Rectangle">
                        <Setter Property="Height" Value="5"/>
                        <Setter Property="Width" Value="5"/>
                        <Setter Property="Fill" Value="DodgerBlue"/>
                        <Setter Property="HorizontalAlignment" Value="Left"/>
                        <Setter Property="VerticalAlignment" Value="Top"/>
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <Trigger Property="hc:TipElement.Placement" Value="LeftTop">
                                <Setter Property="Margin" Value="-3, -3, 0, 0"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="VerticalAlignment" Value="Top"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="Top">
                                <Setter Property="Margin" Value="0, -3, 0, 0"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Top"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="RightTop">
                                <Setter Property="Margin" Value="0, -3, -3, 0"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Top"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="Right">
                                <Setter Property="Margin" Value="0, 0, -3, 0"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="RightBottom">
                                <Setter Property="Margin" Value="0, 0, -3, -3"/>
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="VerticalAlignment" Value="Bottom"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="Bottom">
                                <Setter Property="Margin" Value="0, 0, 0, -3"/>
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Bottom"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="LeftBottom">
                                <Setter Property="Margin" Value="-3, 0, 0, -3"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="VerticalAlignment" Value="Bottom"/>
                            </Trigger>
                            <Trigger Property="hc:TipElement.Placement" Value="Left">
                                <Setter Property="Margin" Value="-3, 0, 0, 0"/>
                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsSelecting}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsDrawing}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </hc:SimplePanel.Resources>
                <Rectangle hc:TipElement.Placement="LeftTop" />
                <Rectangle hc:TipElement.Placement="Top"/>
                <Rectangle hc:TipElement.Placement="RightTop" />
                <Rectangle hc:TipElement.Placement="Right" />
                <Rectangle hc:TipElement.Placement="RightBottom" />
                <Rectangle hc:TipElement.Placement="Bottom" />
                <Rectangle hc:TipElement.Placement="LeftBottom" />
                <Rectangle hc:TipElement.Placement="Left" />
                <!--<TextBlock Text="{TemplateBinding SizeStr}" Background="#CC212121" Foreground="White" HorizontalAlignment="Left" VerticalAlignment="Top" Padding="8,2"/>-->
            </hc:SimplePanel>
        </Border>
        <Border x:Name="PART_Magnifier" IsHitTestVisible="False" Visibility="Collapsed" HorizontalAlignment="Left" VerticalAlignment="Top" Width="122" Height="122" BorderThickness="1" BorderBrush="Transparent" CornerRadius="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="2" Opacity=".2" Direction="-45" RenderingBias="Performance" ShadowDepth="3"/>
            </Border.Effect>
            <hc:SimplePanel>
                <Border Margin="0,0,0,32"  BorderThickness="2" BorderBrush="White">
                    <hc:SimplePanel>
                        <Path Data="M56,0 L60,0 60,40 116,40 116,44 60,44 60,84 56,84 56,44 0,44 0,40 56,40 z" Fill="Black"/>
                    </hc:SimplePanel>
                </Border>
                <Border Height="32" VerticalAlignment="Bottom" Background="Transparent">
                    <StackPanel VerticalAlignment="Center" Margin="5,0,0,0">
                        <TextBlock Foreground="White" />
                    </StackPanel>
                </Border>
            </hc:SimplePanel>
        </Border>
    </hc:SimplePanel>

</hc:Window>
