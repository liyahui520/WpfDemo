<hc:Window x:Class="WpfMain.Controlls.FrmImgView"
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
           xmlns:local="clr-namespace:WpfMain.Controlls"
           xmlns:hc="https://handyorg.github.io/handycontrol"
           xmlns:entity="clr-namespace:WpfMain.Entity"
           ShowNonClientArea="False"
           ShowTitle="True" 
           WindowStartupLocation="CenterScreen"  WindowState="Maximized"
           mc:Ignorable="d" WindowStyle="None"   DataContext="{Binding }">
    <hc:Window.Background>
        <ImageBrush ImageSource="pack://SiteOfOrigin:,,,/Static/Images/background.jpg"  Opacity="0.9"/>
    </hc:Window.Background>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" hc:ScrollViewer.IsInertiaEnabled="False" HorizontalContentAlignment="Center" VerticalContentAlignment="Top" Padding="20,0,0,0" BorderThickness="0" ItemsPanel="{StaticResource FluidMoveBehaviorWrapPanelItemsPanelTemplate}" Style="{StaticResource WrapPanelHorizontalListBox}"  ItemsSource="{Binding Result.Images}">
            <ListBox.Background>
                <SolidColorBrush Color="White" Opacity="0.2"/>
            </ListBox.Background>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type entity:ImageItem}">
                    <hc:Card  BorderBrush="CornflowerBlue" BorderThickness="2" Cursor="Hand" Margin="8" Effect="{StaticResource EffectShadow2}" MaxWidth="140"  Tag="{Binding}"  Footer="{Binding Name}" MouseLeftButtonDown="UIElement_OnMouseLeftButtonDown">
                   
                        <hc:Card.Background>
                            <SolidColorBrush Color="White" Opacity="0.2"/>
                        </hc:Card.Background>
                        <Border CornerRadius="4,4,0,0" Style="{StaticResource BorderClip}">
                            <Image Width="120" Height="100" Source="{Binding ImageSource}" Stretch="Uniform"/>
                        </Border>
                        <hc:Card.FooterTemplate>
                            <DataTemplate>
                                <StackPanel Margin="1">
                                    <TextBlock TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" Style="{StaticResource TextBlockDefault}" Foreground="White" Text="{Binding }" HorizontalAlignment="Center"/>
                                </StackPanel>
                            </DataTemplate>
                        </hc:Card.FooterTemplate>
                    </hc:Card>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel.Background>
                <SolidColorBrush Color="White" Opacity="0.2"/>
            </StackPanel.Background>
            <hc:ImageViewer VerticalAlignment="Center" HorizontalAlignment="Center" Background="{DynamicResource SecondaryRegionBrush}" x:Name="imgView"  Padding="30"/>
         
        </StackPanel>
        <DockPanel Grid.Column="1" Dock="Top" Width="35" Height="35" VerticalAlignment="Top" HorizontalAlignment="Right">
            <Button Margin="5" hc:IconElement.Geometry="{StaticResource CloseGeometry}" Click="ButtonBase_OnClick" Width="30" Style="{StaticResource ButtonOpacityCustom}" hc:BorderElement.CornerRadius="5" Foreground="White" Effect="{StaticResource EffectShadow5}" BorderThickness="0"/>
        </DockPanel>
    </Grid>
</hc:Window>
