<UserControl x:Class="WpfMain.Controlls.UCMFVideo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfMain.Controlls"
          xmlns:controls="clr-namespace:WPFMediaKit.DirectShow.Controls;assembly=WPFMediaKit"
        xmlns:players="clr-namespace:WPFMediaKit.DirectShow.MediaPlayers;assembly=WPFMediaKit"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
            >
    <UserControl.Resources>
        <local:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
    </UserControl.Resources>
    <Grid x:Name="camp">
        <Border Margin="4" x:Name="Border">

            <controls:VideoCaptureElement x:Name="cameraCaptureElement" Grid.Row="1" EnableSampleGrabbing="False"  FPS="30"  LoadedBehavior="Play"
                                          DeeperColor="{Binding IsChecked, ElementName=chkDeeperColor}"
                                          />
        </Border>
    </Grid>
</UserControl>
