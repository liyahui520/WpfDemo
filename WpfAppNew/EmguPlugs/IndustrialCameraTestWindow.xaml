<Window x:Class="WpfAppNew.EmguPlugs.IndustrialCameraTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:WpfAppNew.EmguPlugs"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="工业相机测试窗口 - 显微镜控制系统" 
        Height="900" 
        Width="1400"
        MinHeight="600" 
        MinWidth="1000"
        WindowStartupLocation="CenterScreen" >

    <Window.Resources>
        <!-- 样式资源 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>
        
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        
        <Style x:Key="ValueTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2196F3"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="工业相机测试窗口" Style="{StaticResource HeaderTextStyle}" VerticalAlignment="Center"/>
                    <TextBlock Text="显微镜控制系统" FontSize="14" Foreground="#888888" VerticalAlignment="Center" Margin="20,0,0,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="HelpButton" Content="帮助" Style="{StaticResource ButtonDefault}" Margin="0,0,10,0" Click="HelpButton_Click"/>
                    <Button x:Name="SettingsButton" Content="设置" Style="{StaticResource ButtonDefault}" Click="SettingsButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 主内容区域 -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- 左侧：工业相机控制界面 -->
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="15,10">
                        <TextBlock Text="工业相机控制" Style="{StaticResource HeaderTextStyle}"/>
                    </Border>
                    
                    <local:IndustrialCameraControl Grid.Row="1" x:Name="CameraControl" Margin="10"/>
                </Grid>
            </Border>

            <!-- 分隔线 -->
            <GridSplitter Grid.Column="1" Width="2" Background="#E0E0E0" HorizontalAlignment="Center"/>

            <!-- 右侧：系统信息和控制面板 -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- 系统状态 -->
                <Border Grid.Row="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
                    <StackPanel Margin="15">
                        <TextBlock Text="系统状态" Style="{StaticResource HeaderTextStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="系统时间：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="SystemTimeText" Text="{Binding SystemTime}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="CPU使用率：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="CpuUsageText" Text="{Binding CpuUsage}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="内存使用：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="MemoryUsageText" Text="{Binding MemoryUsage}" Style="{StaticResource ValueTextStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 图像信息 -->
                <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
                    <StackPanel Margin="15">
                        <TextBlock Text="图像信息" Style="{StaticResource HeaderTextStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="分辨率：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ResolutionText" Text="{Binding ImageResolution}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="帧率：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="FrameRateText" Text="{Binding FrameRate}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="格式：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="FormatText" Text="{Binding ImageFormat}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="3" Grid.Column="0" Text="文件大小：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" x:Name="FileSizeText" Text="{Binding FileSize}" Style="{StaticResource ValueTextStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 显微镜状态 -->
                <Border Grid.Row="2" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
                    <StackPanel Margin="15">
                        <TextBlock Text="显微镜状态" Style="{StaticResource HeaderTextStyle}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="连接状态：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" x:Name="ConnectionStatusText" Text="{Binding ConnectionStatus}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="预览状态：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" x:Name="PreviewStatusText" Text="{Binding PreviewStatus}" Style="{StaticResource ValueTextStyle}"/>
                            
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="录制状态：" Style="{StaticResource InfoTextStyle}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" x:Name="RecordingStatusText" Text="{Binding RecordingStatus}" Style="{StaticResource ValueTextStyle}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- 快速操作 -->
                <Border Grid.Row="3" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
                    <StackPanel Margin="15">
                        <TextBlock Text="快速操作" Style="{StaticResource HeaderTextStyle}"/>
                        <UniformGrid Columns="2" Margin="0,10,0,0">
                            <Button Content="重置设置" Style="{StaticResource ButtonDefault}" Margin="0,0,5,5"/>
                            <Button Content="保存配置" Style="{StaticResource ButtonDefault}" Margin="5,0,0,5"/>
                            <Button Content="加载配置" Style="{StaticResource ButtonDefault}" Margin="0,5,5,0"/>
                            <Button Content="导出日志" Style="{StaticResource ButtonDefault}" Margin="5,5,0,0"/>
                        </UniformGrid>
                    </StackPanel>
                </Border>

                <!-- 操作日志 -->
                <Border Grid.Row="4" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="5">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <Border Grid.Row="0" Background="#F8F9FA" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="15,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="操作日志" Style="{StaticResource HeaderTextStyle}"/>
                                <Button Grid.Column="1" x:Name="ClearLogButton" Content="清空" Style="{StaticResource ButtonDefault}" Click="ClearLogButton_Click"/>
                            </Grid>
                        </Border>
                        
                        <ScrollViewer Grid.Row="1" x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" Padding="10">
                            <TextBlock x:Name="LogTextBlock" Text="{Binding LogText}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="10" Foreground="#333333"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <!-- 状态栏 -->
        <Border Grid.Row="2" Background="#F5F5F5" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="就绪" FontSize="12" Foreground="#666666" VerticalAlignment="Center"/>
                    <Rectangle Width="1" Height="12" Fill="#E0E0E0" Margin="15,0"/>
                    <TextBlock x:Name="StatusText" Text="{Binding StatusMessage}" FontSize="12" Foreground="#666666" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="版本 1.0.0" FontSize="12" Foreground="#888888" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>