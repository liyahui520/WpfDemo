<UserControl x:Class="WpfAppNew.OpenCV.Controls.CameraPreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="CameraPreviewControl_Loaded"
             Unloaded="CameraPreviewControl_Unloaded">
    
    <UserControl.Resources>
        <!-- 布尔值到可见性转换器 -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- 反向布尔值到可见性转换器 -->
        <Style x:Key="InvertedBooleanToVisibilityConverter" TargetType="FrameworkElement">
            <Setter Property="Visibility" Value="Visible"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding}" Value="True">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <!-- 主要内容区域 -->
        <Border Background="Black" CornerRadius="5">
            <Grid>
            <!-- 视频显示区域 -->
            <Viewbox Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image x:Name="VideoImage" 
                       Source="{Binding CurrentFrame, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       Stretch="Uniform"
                       RenderOptions.BitmapScalingMode="HighQuality"/>
            </Viewbox>
            
            <!-- 无信号提示 -->
            <!--<Grid Visibility="{Binding HasSignal, Converter={StaticResource InvertedBooleanToVisibilityConverter}}">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasSignal, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="📷" FontSize="48" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="无信号" FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding HasSignal}" FontSize="16" Foreground="Gray" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                               FontSize="12" Foreground="LightGray" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                </StackPanel>
            </Grid>-->
                </Grid>
        </Border>
        
        <!-- 录制指示器 -->
        <Border x:Name="RecordingIndicator" 
                Background="Red" 
                CornerRadius="10" 
                Width="20" Height="20" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Top" 
                Margin="10"
                Visibility="{Binding IsRecording, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border.RenderTransform>
                <ScaleTransform x:Name="RecordingScale" ScaleX="1" ScaleY="1"/>
            </Border.RenderTransform>
            <Border.Triggers>
                <EventTrigger RoutedEvent="Border.Loaded">
                    <BeginStoryboard>
                        <Storyboard RepeatBehavior="Forever">
                            <DoubleAnimation Storyboard.TargetName="RecordingScale" 
                                           Storyboard.TargetProperty="ScaleX" 
                                           From="1" To="1.2" Duration="0:0:0.5" 
                                           AutoReverse="True"/>
                            <DoubleAnimation Storyboard.TargetName="RecordingScale" 
                                           Storyboard.TargetProperty="ScaleY" 
                                           From="1" To="1.2" Duration="0:0:0.5" 
                                           AutoReverse="True"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Border.Triggers>
        </Border>
        
        <!-- 拍照闪光效果 -->
        <Border x:Name="FlashEffect" 
                Background="White" 
                Opacity="0" 
                Visibility="Collapsed"/>
    </Grid>
</UserControl>