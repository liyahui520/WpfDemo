<Window x:Class="WpfAppNew.Windows.TestVideoPlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="视频播放器测试" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#FF2D2D30">
    
    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="TestButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 文本样式 -->
        <Style x:Key="InfoTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- 标题样式 -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FF007ACC"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <TextBlock Text="视频播放器对比测试" Style="{StaticResource TitleTextStyle}" FontSize="20"/>
        </StackPanel>

        <!-- 播放器对比区域 -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- VLC播放器区域 -->
            <Border Grid.Column="0" BorderBrush="#FF007ACC" BorderThickness="2" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="VLC播放器" Style="{StaticResource TitleTextStyle}"/>
                    
                    <Border Grid.Row="1" Name="vlcPlayerContainer" Background="Black" Margin="5">
                        <!-- VLC播放器将在这里动态加载 -->
                    </Border>
                    
                    <TextBlock Grid.Row="2" Name="vlcInfoText" Text="VLC: 未初始化" 
                             Style="{StaticResource InfoTextStyle}" HorizontalAlignment="Center"/>
                </Grid>
            </Border>

            <!-- OpenCV播放器区域 -->
            <Border Grid.Column="2" BorderBrush="#FF007ACC" BorderThickness="2" CornerRadius="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="OpenCV播放器" Style="{StaticResource TitleTextStyle}"/>
                    
                    <Border Grid.Row="1" Name="openCvPlayerContainer" Background="Black" Margin="5">
                        <!-- OpenCV播放器将在这里动态加载 -->
                    </Border>
                    
                    <TextBlock Grid.Row="2" Name="openCvInfoText" Text="OpenCV: 未初始化" 
                             Style="{StaticResource InfoTextStyle}" HorizontalAlignment="Center"/>
                </Grid>
            </Border>
        </Grid>

        <!-- 控制按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Name="selectFileButton" Content="选择视频文件" 
                    Style="{StaticResource TestButtonStyle}" 
                    Click="SelectFileButton_Click"/>
            <Button Name="playBothButton" Content="同时播放" 
                    Style="{StaticResource TestButtonStyle}" 
                    Click="PlayBothButton_Click"/>
            <Button Name="pauseBothButton" Content="同时暂停" 
                    Style="{StaticResource TestButtonStyle}" 
                    Click="PauseBothButton_Click"/>
            <Button Name="stopBothButton" Content="同时停止" 
                    Style="{StaticResource TestButtonStyle}" 
                    Click="StopBothButton_Click"/>
        </StackPanel>

        <!-- 性能信息区域 -->
        <Border Grid.Row="3" Background="#FF3C3C3C" Margin="10" CornerRadius="5" Padding="10">
            <StackPanel>
                <TextBlock Text="性能监控信息" Style="{StaticResource TitleTextStyle}" FontSize="14"/>
                <TextBlock Name="performanceText" Text="等待测试开始..." 
                         Style="{StaticResource InfoTextStyle}" HorizontalAlignment="Center"/>
                <TextBlock Text="对比说明：VLC播放器成熟稳定但资源占用较高；OpenCV播放器轻量级且性能优化好，适合宠物行业应用" 
                         Style="{StaticResource InfoTextStyle}" HorizontalAlignment="Center" 
                         FontSize="10" Foreground="#FFAAAAAA" Margin="5,10,5,5"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>